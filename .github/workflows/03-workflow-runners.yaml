name: 03 - Workflow runners

on: push

jobs:
    ubuntu-echo:
        runs-on: ubuntu-latest
        steps:
           - name: Show OS #executa script bash
             run: |
              echo "This is running on an Ubuntu runner."
              echo "Runner OS: $RUNNER_OS"